[package]
name = "pathfinding_algorithms"
version = "1.0.0"
edition = "2021"

[dependencies]
env_logger = "0.11" # Инициализатор, который выводит логи в консоль с цветами
log = "0.4"
piston_window = "0.132.0"
rand = "0.9.0-alpha.1"
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"
